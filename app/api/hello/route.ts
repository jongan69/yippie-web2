import { NextResponse } from 'next/server'

export async function GET(request: Request) {
  return NextResponse.json({ msg: 'Where the FUCK is my $mememe I have been WAITING for DAYS. I cannot wait any longer my bonkbot is loaded up and ready to ape. I just snorted a line and im ready to go to valhalla with my bags.\nNow give me my $mememe or im fudding.' })
}